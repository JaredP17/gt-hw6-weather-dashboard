$(document).ready(function () {
    // VARIABLES
    var now = moment().format("(M/D/YYYY)");

    console.log(now);
    // FUNCTION DEFINITIONS

    // FUNCTION CALLS

    // EVENT LISTENERS
   
});


